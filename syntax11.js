/* 
Scope - Variable lifetime

Lexical scoping (var) - from when they are declared until their function ends
Block scoping (const, let) - until next } is reacher

*/

//reference cannot be changed 
//thats why :
//const obj = {} and then doing something like obj.a = 'a' is possible
//but obj = {} after above line will give an error