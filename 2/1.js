// Classes
// Simplifies the defining of complex objects with their own prototypes
// Classes is an abstract thing that you can declare and when you actually turn an abstract class into an instance of that object which is called instance

Date // is a class 

const d = new Date() // d is an instance

console.log(d)